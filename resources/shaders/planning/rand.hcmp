
// uniforms

uniform uint  uSeed;

// functions

uint generateSeed();
uint iterateSeed(inout uint seed);
float32_t rand(inout uint seed);
float32_t rand(inout uint seed, in float32_t min, in float32_t max);
